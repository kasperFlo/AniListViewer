import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './src/App'; // Assuming you'll eventually have an App component.

const root = ReactDOM.createRoot(document.getElementById('text'));
root.render(<App />);
